# Diff Engine

## Purpose

The Diff Engine performs graph-based comparison between two specifications, generating a structured ChangeSet that classifies differences as breaking or non-breaking.

## Expected Content

- Graph comparison algorithm
- ChangeSet generation and structure
- Breaking change classification rules
- Semantic vs structural diff
- Output formats (JSON, YAML)
